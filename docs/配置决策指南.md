# 配置决策指南

> 如何选择正确的 storage-path 配置

---

## 🎯 核心原则

**只有两种合理的选择**:

1. **不指定路径** → 全局共享记忆
2. **指定项目路径** → 项目隔离记忆

**❌ 避免**: 自定义的全局路径 (如 `~/Documents/AgentMemory`)
- 既不是默认位置，也不是项目路径
- 增加配置复杂度
- 没有明确的使用场景

---

## 📊 决策树

```
需要配置 MCP 记忆系统
    │
    ├─ 你是个人用户，希望所有 AI 应用共享记忆？
    │   └─ 是 → 【选择 A: 全局共享】不指定 storage-path
    │
    └─ 你有多个项目，需要隔离记忆？
        └─ 是 → 【选择 B: 项目隔离】指定项目内路径
```

---

## 选择 A: 全局共享记忆

### 适用场景

✅ **推荐给**:
- 个人用户
- 希望跨应用记忆互通
- 统一的知识管理
- 学习笔记和个人知识库

❌ **不适合**:
- 多客户项目
- 需要隐私隔离
- 团队协作项目

### 配置方式

**VS Code / Cline / Roo Code**:
```json
{
  "mcpServers": {
    "adaptive-agent-mcp": {
      "command": "uvx",
      "args": ["adaptive-agent-mcp"]
    }
  }
}
```

**Claude Code**:
```bash
claude mcp add adaptive-memory \
  --type stdio \
  --command uvx \
  --args "adaptive-agent-mcp"
```

### 记忆位置

```
Windows: C:\Users\YourName\.adaptive-agent\memory
macOS:   /Users/YourName/.adaptive-agent/memory
Linux:   /home/yourname/.adaptive-agent/memory
```

### 效果

```
┌─────────────────┐
│   VS Code       │
│   项目 A        │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────┐
│  ~/.adaptive-agent/memory       │  ← 全局共享
│  (所有应用共享)                 │
└─────────────────────────────────┘
         ▲
         │
┌────────┴────────┐
│  Claude Code    │
│  项目 B         │
└─────────────────┘
```

**示例**:
```
在 VS Code 项目 A 中: "记住，我喜欢 TypeScript"
在 Claude Code 项目 B 中: "我的编程偏好是什么？"
AI: "你喜欢 TypeScript" ✅ 记忆互通！
```

---

## 选择 B: 项目隔离记忆

### 适用场景

✅ **推荐给**:
- 多客户项目
- 工作/个人分离
- 需要隐私保护
- 团队协作 (记忆可加入 Git)

❌ **不适合**:
- 希望跨项目共享知识
- 个人学习笔记

### 配置方式

**VS Code / Cline / Roo Code**:
```json
{
  "mcpServers": {
    "adaptive-agent-mcp": {
      "command": "uvx",
      "args": [
        "adaptive-agent-mcp",
        "--storage-path",
        "${workspaceFolder}/agent_memory"
      ]
    }
  }
}
```

**Claude Code**:
```bash
# 注意：Claude Code 不支持变量，需要使用绝对路径
claude mcp add adaptive-memory \
  --type stdio \
  --command uvx \
  --args "adaptive-agent-mcp" \
  --args "--storage-path" \
  --args "~/Projects/my-project/agent_memory"
```

### 记忆位置

```
项目 A: /path/to/project-a/agent_memory/
项目 B: /path/to/project-b/agent_memory/
```

### 效果

```
┌─────────────────┐
│   项目 A        │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────┐
│  project-a/agent_memory/        │
│  (项目 A 专属)                   │
└─────────────────────────────────┘

┌─────────────────┐
│   项目 B        │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────┐
│  project-b/agent_memory/        │
│  (项目 B 专属)                   │
└─────────────────────────────────┘
```

**示例**:
```
在项目 A 中: "记住，客户 A 使用 MySQL"
在项目 B 中: "这个项目用什么数据库？"
AI: "我没有找到相关记忆" ✅ 记忆隔离！
```

---

## ❌ 不推荐: 自定义全局路径

### 为什么不推荐 `~/Documents/AgentMemory`？

**问题**:
1. **不是默认位置**: 需要手动指定，增加配置复杂度
2. **不是项目路径**: 不随项目移动，不能加入 Git
3. **没有明确场景**: 既不是全局共享，也不是项目隔离
4. **维护困难**: 用户需要记住这个自定义路径

**如果你想要全局共享**:
- ✅ 使用默认路径 (不指定 `--storage-path`)
- ❌ 不要自定义 `~/Documents/AgentMemory`

**如果你想要项目隔离**:
- ✅ 使用项目内路径 (`${workspaceFolder}/agent_memory`)
- ❌ 不要使用项目外的路径

---

## 🔄 切换配置

### 从全局共享切换到项目隔离

**步骤**:
1. 备份全局记忆 (可选)
   ```bash
   cp -r ~/.adaptive-agent/memory ~/backup_memory
   ```

2. 更新配置，添加 `--storage-path`
   ```json
   {
     "args": [
       "adaptive-agent-mcp",
       "--storage-path",
       "${workspaceFolder}/agent_memory"
     ]
   }
   ```

3. 重启 IDE

4. (可选) 复制需要的记忆到项目
   ```bash
   cp -r ~/.adaptive-agent/memory/knowledge ./agent_memory/
   ```

### 从项目隔离切换到全局共享

**步骤**:
1. 备份项目记忆 (可选)
   ```bash
   cp -r ./agent_memory ~/backup_project_memory
   ```

2. 更新配置，移除 `--storage-path`
   ```json
   {
     "args": ["adaptive-agent-mcp"]
   }
   ```

3. 重启 IDE

4. (可选) 合并项目记忆到全局
   ```bash
   cp -r ./agent_memory/knowledge ~/.adaptive-agent/memory/
   ```

---

## 📝 配置检查清单

### 全局共享配置

- [ ] 配置中**没有** `--storage-path` 参数
- [ ] 重启 IDE 后，记忆文件出现在 `~/.adaptive-agent/memory/`
- [ ] 在不同项目中都能访问相同的记忆

### 项目隔离配置

- [ ] 配置中**有** `--storage-path` 参数
- [ ] 路径指向项目内 (如 `${workspaceFolder}/agent_memory`)
- [ ] 重启 IDE 后，记忆文件出现在项目目录下
- [ ] 不同项目的记忆互不影响

---

## 🎓 最佳实践

### 个人用户

**推荐**: 全局共享

```json
{
  "command": "uvx",
  "args": ["adaptive-agent-mcp"]
}
```

**理由**:
- 简单，无需配置路径
- 知识积累在一个地方
- 跨项目的上下文连续性

### 专业开发者 (多项目)

**推荐**: 项目隔离

```json
{
  "command": "uvx",
  "args": [
    "adaptive-agent-mcp",
    "--storage-path",
    "${workspaceFolder}/agent_memory"
  ]
}
```

**理由**:
- 客户项目隔离
- 可以加入 Git (团队共享)
- 隐私保护

### 混合模式 (高级)

**场景**: 既要全局知识库，又要项目隔离

**方案**: 使用两个 MCP 实例

```json
{
  "mcpServers": {
    "global-memory": {
      "command": "uvx",
      "args": ["adaptive-agent-mcp"]
    },
    "project-memory": {
      "command": "uvx",
      "args": [
        "adaptive-agent-mcp",
        "--storage-path",
        "${workspaceFolder}/agent_memory"
      ]
    }
  }
}
```

**使用**:
- 通用知识: 调用 `global-memory` 的工具
- 项目记忆: 调用 `project-memory` 的工具

---

## 常见问题

### Q: 我可以使用 `~/Documents/AgentMemory` 吗？

**A**: 技术上可以，但**不推荐**。

如果你想要全局记忆，使用默认路径更简单：
```json
{"args": ["adaptive-agent-mcp"]}
```

如果你想要项目隔离，使用项目路径更合理：
```json
{"args": ["adaptive-agent-mcp", "--storage-path", "${workspaceFolder}/agent_memory"]}
```

### Q: 默认路径在哪里？

**A**: `~/.adaptive-agent/memory`

```
Windows: C:\Users\YourName\.adaptive-agent\memory
macOS:   /Users/YourName/.adaptive-agent\memory
Linux:   /home/yourname/.adaptive-agent/memory
```

### Q: 如何查看当前使用的路径？

**A**: 启动 MCP 服务器时会显示：

```
Starting Adaptive-Agent-MCP v1.0.0
Storage path: /Users/yourname/.adaptive-agent/memory
```

或在对话中询问 AI：
```
你: 当前的记忆存储路径是什么？
AI: (调用 initialize_session 后会显示路径)
```

### Q: 可以同时使用多个路径吗？

**A**: 可以，配置多个 MCP 实例，每个使用不同的路径。

---

## 总结

### 简单决策

```
个人用户 → 不指定路径 (全局共享)
多项目  → 指定项目路径 (项目隔离)
```

### 配置模板

**全局共享**:
```json
{"args": ["adaptive-agent-mcp"]}
```

**项目隔离**:
```json
{"args": ["adaptive-agent-mcp", "--storage-path", "${workspaceFolder}/agent_memory"]}
```

**就这么简单！** 🎉
